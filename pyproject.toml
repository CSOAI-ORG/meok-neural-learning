[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[project]
name = "meok-neural-learning"
version = "1.0.0"
description = "MEOK AI Labs — Shared neural learning for MCP servers. Every tool call makes the next one smarter."
readme = "README.md"
license = {file = "LICENSE"}
requires-python = ">=3.10"
authors = [{name = "MEOK AI Labs", email = "nicholas@meok.ai"}]
keywords = ["mcp", "neural-network", "learning", "ai", "meok", "self-improving"]
dependencies = []
[project.urls]
Homepage = "https://meok.ai"
Repository = "https://github.com/CSOAI-ORG/meok-neural-learning"
